<%= form_for @sakaya, :url => sakaya_path(@sakaya), :method => :patch do |f| %>
		<%= render :partial => 'form', :locals => { :f => f } %>
    <%= f.submit "Update", :class =>"btn btn-danger" %>
<% end %>

